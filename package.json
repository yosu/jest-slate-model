{
  "name": "jest-slate-model",
  "version": "0.1.0",
  "description": "A custom matcher and editor initializer for slatejs.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yosu",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
